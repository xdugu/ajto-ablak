import { ShippingProviderPipe } from './shipping-provider.pipe';

describe('ShippingProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new ShippingProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
