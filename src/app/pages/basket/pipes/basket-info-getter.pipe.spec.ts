import { BasketInfoGetterPipe } from './basket-info-getter.pipe';

describe('BasketInfoGetterPipe', () => {
  it('create an instance', () => {
    const pipe = new BasketInfoGetterPipe();
    expect(pipe).toBeTruthy();
  });
});
