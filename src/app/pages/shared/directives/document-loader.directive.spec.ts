import { DocumentLoaderDirective } from './document-loader.directive';

describe('DocumentLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
