import { FlowImagePipe } from './flow-image.pipe';

describe('FlowImagePipe', () => {
  it('create an instance', () => {
    const pipe = new FlowImagePipe();
    expect(pipe).toBeTruthy();
  });
});
